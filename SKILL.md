---
name: produce-voiceover-mg-social-video
description: 把一个信息、概念、观点或产品流程制作成带用户原声、信息型 MG 动效、同步字幕、双平台封面和发布文案的小红书/抖音完整视频包；已配置授权音色 API 时直接生成和下载旁白。适用于没有真人出镜、以动态图形而非纯录屏为主的解释、对比、清单、流程和演示视频；纯录屏剪辑优先使用录屏视频专项流程。
---

# 原声 MG 社媒视频

把用户给出的主题和原声推进到可直接发布的竖屏成片。默认同时适配小红书与抖音，保留原始素材，实际生成视频、字幕、封面与发布包，不把制作步骤留给用户。

## 先选择表达模式

- **信息解释**：回答“发生了什么、为什么与我有关、结论是什么”。
- **概念说明**：用一个具体例子解释抽象概念，避免术语堆叠。
- **流程演示**：展示输入、执行、状态变化与结果；演示数据必须标明，不能冒充真实运行证据。

需要设计叙事时读取 [references/story-and-motion.md](references/story-and-motion.md)。以录屏为主体时可以混入 MG 标注，但不要把静态截图轮播伪装成 MG 视频。

## 输入与默认决策

优先取得：主题、受众、唯一结论、事实依据、一个 CTA、用户原声或口播稿、视觉参考、目标平台。信息足够时直接制作；只有口播方向、事实边界或外部授权会实质改变结果时才询问。

- 用户要求使用本人的声音时，优先使用其提供的音频。
- 用户已经明确授权某个平台、账号和个人音色用于持续制作，且本机存在该平台的 API 配置时，默认直接通过 API 生成和下载旁白，不再要求用户手工复制、生成或下载。不要为每一期重复询问同一项已建立的音色使用授权。
- 需要生成克隆音色时，必须使用用户明确授权的平台、账号与音色；不得复用对话中暴露的旧密钥，不得从浏览器存储、Cookie、页面源码或历史对话中提取密钥。
- API 密钥只能从进程环境、系统环境或当前运行环境提供的密钥管理能力读取；音色 ID/名称也应作为运行配置，不写死在 Skill、模板或项目工程中。
- 已配置 MossAPI 时读取 [references/moss-api.md](references/moss-api.md)，使用 `scripts/moss_tts.py` 查询授权音色并合成。API 是默认路径；只有 API 不可用、用户仍有已登录网页且继续制作不会改变事实口径时，才使用浏览器界面降级。
- 用户尚未提供音频时，先交付可直接合成的口播定稿，再继续画面模板与封面，不阻塞并行工作。
- 音频已生成后，不擅自改写口播；事实问题优先用画面限定语修正，必须改口播时明确说明需要重录的句子。

## 制作主流程

1. 建立 Brief：目标、受众、核心问题、唯一结论、事实/推断边界、CTA、平台和视觉方向。
2. 核验会变化或容易误解的事实；画面与口播区分官方事实、组合示例、作者判断和未验证目标。
3. 写口播：前 3 秒给出问题或反差；每句话对应一个视觉阶段；删掉功能目录式堆砌。
4. 取得原声并制作逐句时码：优先使用用户提供的原始音频；若已配置用户授权音色 API，则直接按口播定稿生成、下载并保留 API 原始输出。详细规则见 [references/audio-and-timing.md](references/audio-and-timing.md)。
5. 按时码设计 6–9 个场景，并先完成场景级“口播—画面覆盖表”；每屏一个主结论，每个动画都要表达信息变化。覆盖表的写法与判断规则见 [references/story-and-motion.md](references/story-and-motion.md)。
6. 使用确定性时间驱动的 HTML/CSS/SVG/Canvas 或当前环境可用的等价方式制作 MG；优先暴露 renderAt(t)，按绝对时间逐帧渲染，避免依赖墙钟时间的录屏导致掉帧或音画漂移。
7. 烧录同步字幕，加入稀疏的语义音效；没有音乐授权时交付无背景音乐母版并给平台内选曲建议。
8. 分别制作封面和发布包。规格与字段见 [references/delivery-and-qa.md](references/delivery-and-qa.md)。
9. 完成技术、内容、视觉动效、字幕音频四类验收后再写入最终交付目录。

## 不可退化的制作标准

- 不是静态图片切换、联系表、PPT 推拉或随机漂浮元素。
- 动效只用于：需求出现、命令/步骤被调用、状态改变、结果回写、关系建立和章节切换。
- 时间同步不等于内容同步：场景标题、核心画面和同一时段口播必须回答同一个问题。主画面不得新增口播未解释的选择规则、步骤、能力、结论或限制；缺少口播覆盖时应删除、降为非关键辅助信息，或明确要求补录。
- 抽象表达改成用户能立即理解的动词，例如“约时间、找资料、改数据、走流程”。
- 主画面与字幕区分层；常用竖屏底部预留约 220–300px，避免与平台 UI 和 CTA 重叠。
- 画面出现真实命令、数据、接口或产品能力时必须可核验；组合流程写“示例/目标/可组合”，不能暗示已端到端跑通。
- 封面必须独立排版，不把竖图硬裁成横图。

## 原声与字幕底线

- 永远保留原始音频副本。
- API 合成返回的原始文件视同原声母本：先保存并记录哈希、生成模型/版本、音色标识、生成时间和口播稿版本，再做剪辑、响度或变速处理。
- 不把“请求已发出”当作音频完成。必须确认响应为可解码音频、文件非空、时长合理并实际试听首段、中段和片尾；异步接口要等任务成功且结果下载完成。
- 口播定稿变更后不得沿用旧音频；按新稿重新生成，并保留可追溯的稿件—音频对应关系。
- 默认只从明确静音的中段收紧气口；保留语义停顿，剪点靠近零交叉或使用短交叉淡化。
- 累计提速通常不超过约 1.20×；英文缩写、句尾或情绪明显变机械时立即降低。
- 成片旁白建议约 -16 LUFS，真峰值不高于 -1.5 dBTP；音效必须明显低于人声。
- 字幕以逐句/逐词发声锚点为基础，再用口播定稿纠错；不得把品牌名和英文词错误拆行。

## 交付与授权

最终按平台输出中文命名文件，至少包括成片、封面、口播稿、SRT 和发布包。平台要求横封面时同时输出 3:4 竖封面与 4:3 横封面。具体清单见 [references/delivery-and-qa.md](references/delivery-and-qa.md)。

公开发布属于外部操作：只有用户明确要求发布，并在看过最终成片、封面和文案后，才执行上传或发布。制作授权不等于发布授权。

## 可复用资源

- 新建项目时可运行 scripts/init_project.py <project-dir> --title "主题" --mode information|concept|demo。
- MossAPI 已配置时可运行 `python scripts/moss_tts.py synthesize --input-file <口播稿> --output <原始音频>`；音色从 `MOSS_VOICE_ID` 或 `MOSS_VOICE_NAME` 解析，密钥从 `MOSS_API_KEY` 读取。
- 首次核对音色可运行 `python scripts/moss_tts.py list-voices --match <音色名称>`，确认返回的名称与 ID 后再生成完整口播。
- 完成后运行 scripts/validate_delivery.py <output-dir> --topic "主题" 做交付结构与基础媒体检查。
- assets/mg-template/ 是可复制的确定性 Canvas MG 起点。
- assets/templates/ 包含 Brief、时间轴和发布包模板。

确认成品可用后，清理拆帧、联系表、候选版、临时混音和调试文件；保留最终交付、用户原始素材、事实证据与可复现的时间轴/工程文件。
