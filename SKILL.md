---
name: produce-voiceover-mg-social-video
description: 把一个信息、概念、观点或产品流程制作成带用户原声、信息型 MG 动效、同步字幕、双平台封面和发布文案的小红书/抖音完整视频包；已配置授权音色 API 时直接生成和下载旁白。适用于没有真人出镜、以动态图形而非纯录屏为主的解释、对比、清单、流程和演示视频；纯录屏剪辑优先使用录屏视频专项流程。
---

# 原声 MG 社媒视频

把用户给出的主题和原声推进到可直接发布的竖屏成片。默认同时适配小红书与抖音，保留原始素材，实际生成视频、字幕、封面与发布包，不把制作步骤留给用户。

## 运行架构：一个主智能体负责到底

从 Brief、事实核验、口播、音频、时间轴、视觉导演、素材、渲染、终审到交付，全部由当前主智能体连续完成。禁止把案例、语义窗、音频审核、画面审核或终验委派给子智能体，也禁止把多个独立产出拼成一条片。脚本、渲染器、ASR、图像生成、网页截图和媒体检测只作为确定性工具使用，结果必须回到同一个主智能体判断。长片可以分成连续区间逐段检查，但同一负责人必须持有完整 Brief、事实矩阵、术语表、时间轴、素材账本和缺陷账本，并在最后从头到尾 `1×` 完整播放。

新项目使用 `scripts/init_project.py` 时会创建 `project-state.json`；已有项目先运行 `scripts/project_state.py init <project-dir>`。每个阶段的实际产物要先 `record`，完成对应专项验收后才能 `approve`。口播、音频、时间轴、素材或成片一旦变化，旧的下游状态自动变成 `stale`，不得继续沿用旧时码、旧渲染或旧验收结论。完整阶段、依赖、失效传播和命令见 [references/orchestration-and-state.md](references/orchestration-and-state.md)。架构简化不减少下文及各专项 reference 中的任何硬性标准。

## 先选择表达模式

画面实现新增 Remotion 制作层：新工程默认初始化 `assets/remotion-template/`。制作或修改 MG 前读取 [references/remotion-production.md](references/remotion-production.md)，使用实际测字、原图坐标映射的自适应标注、避障箭头和语义锚点驱动组件；原有导演、证据、音频与逐帧终验标准全部保留。已有认可工程不自动迁移。组件诊断片只证明工程行为，正式长片仍先做同稿同音频的 30–60 秒标杆和 0–8 秒开头验收。

- **信息解释**：回答“发生了什么、为什么与我有关、结论是什么”。
- **概念说明**：让零基础观众听完后能复述概念，而不只是听过名词。简明不等于一句定义或泛泛带过；至少讲清它解决的问题、普通话定义、最小工作机制、一个贯穿案例和关键边界，避免术语堆叠。同时必须按媒体叙事组织，不能把这些要素机械写成教科书目录。完整骨架、复述测试与媒体观看测试见 [references/story-and-motion.md](references/story-and-motion.md)。
- **流程演示**：展示输入、执行、状态变化与结果；演示数据必须标明，不能冒充真实运行证据。

需要设计叙事时读取 [references/story-and-motion.md](references/story-and-motion.md)。以录屏为主体时可以混入 MG 标注，但不要把静态截图轮播伪装成 MG 视频。

社媒信息完整不等于逐项报目录。涉及多个案例、功能或清单时，先按观众正在承受的 3–5 组问题、代价或决策冲突重新分组，再把每一项放进这条情绪与认知主线；不能默认“一项一个场景、平均分配时长”。

## 输入与默认决策

优先取得：主题、受众、唯一结论、事实依据、主行动目标、用户原声或口播稿、视觉参考、目标平台。社媒口播默认还要设计一个开头轻量动作钩子和一个片尾评论问题；信息足够时直接制作，下一期题目未定时不询问创作者，直接把片尾评论设计成选题征集。只有口播方向、事实边界或外部授权会实质改变结果时才询问。

- 用户要求使用本人的声音时，优先使用其提供的音频。
- 用户已经明确授权某个平台、账号和个人音色用于持续制作，且本机存在该平台的 API 配置时，默认直接通过 API 生成和下载旁白，不再要求用户手工复制、生成或下载。不要为每一期重复询问同一项已建立的音色使用授权。
- 需要生成克隆音色时，必须使用用户明确授权的平台、账号与音色。用户在当前任务中主动发送 API Key 并明确要求直接使用时，允许把该值作为一次性运行凭证注入合成进程；不要仅因它出现在当前对话里就拒绝执行，也不要重复要求用户改用终端输入。
- 用户明确要求同一凭证供后续视频持续使用时，优先保存到操作系统密钥管理器并在后续任务自动读取；没有持续授权时只在当前任务内使用。不得把密钥写入 Skill、项目文件、普通配置、发布包或回复正文，也不得从未获当前授权的历史对话、浏览器存储、Cookie、页面源码或网络请求中搜集凭证。
- API 密钥可来自用户当前任务中的明确直发、进程/系统环境或当前运行环境提供的密钥管理能力。传递时关闭命令回显和调试输出，不在错误信息中打印密钥；音色 ID/名称也作为运行配置，不写死在 Skill、模板或项目工程中。
- 已配置 MossAPI 时读取 [references/moss-api.md](references/moss-api.md)，使用 `scripts/moss_tts.py` 查询授权音色并合成。API 是默认路径；只有 API 不可用、用户仍有已登录网页且继续制作不会改变事实口径时，才使用浏览器界面降级。
- 用户尚未提供音频时，先交付可直接合成的口播定稿，再继续画面模板与封面，不阻塞并行工作。
- 音频已生成后，不擅自改写口播；事实问题优先用画面限定语修正，必须改口播时明确说明需要重录的句子。

## 制作主流程

1. 建立 Brief：目标、受众、核心问题、唯一结论、事实/推断边界、主行动目标、开头动作钩子、片尾评论问题、平台和视觉方向。前三秒必须分别写清口播钩子和视觉钩子；视觉钩子包含“第一视觉焦点、可见变化、信息回报”。涉及具体产品时，还要登记产品第一次被念到的发声锚点和同步出现的官方识别素材。
2. 核验会变化或容易误解的事实；画面与口播区分官方事实、组合示例、作者判断和未验证目标。
3. 写口播：前 3 秒用问题、反差、误解或直接后果给出观看理由；每句话对应一个视觉阶段；删掉功能目录式堆砌。定稿必须能标出“观众痛点 → 代价放大 → 意外发现/认知翻转 → 证据缓解 → 可记忆结论”，不要求固定顺序报栏目，但不得缺少情绪起点和变化。概念说明必须具备“问题、定义、机制、案例、边界、可复述结论”的完整认知链，但优先让贯穿案例和层层追问承载这些内容，不能机械报栏目。讲官网案例、产品实践或官方操作文档时，必须先按 [references/official-case-explanation.md](references/official-case-explanation.md) 建立逐案例证据矩阵；每个案例都要完整填写 `available_options`、`official_path`、`creator_preference`、`selection_conditions`、`steps`、`image_evidence`、`result`、`limits`。先说明产品有哪些可选路径，再还原官网案例实际选择的路径，最后表达创作者在什么条件下会怎样选择；不得把官网示范路径写成唯一解、产品默认最佳实践或创作者本人的判断。全片围绕一个核心问题持续推进，中段至少发生一次认知推进或误解翻转，结尾回扣开头。不能用一句口号、几个同义词或一串功能代替解释。口播必须是直接对观众说的话，不朗读“画面只是示意”“如图所示”“这里可以看到”等制作说明；事实边界改成自然表达，例如“具体处理方式取决于模型和产品”。不得把官网字段或验收名词直接串成一句话，例如“顶层文件、隐藏配置目录、下级文件夹未改动”；先说观众熟悉的对象和动作，例如“只改了当前这一层，里面的文件夹没碰”。画面短标题也不得用“名词 + 名词 + 状态”的报告式压缩代替正常表达。合成前逐句做朗读测试：一句话如果日常聊天中说不出口，就先改成人话再合成；可运行 `scripts/check_audience_language.py <口播稿> <最终字幕>`。
   口播定稿还必须按 [references/voiceover-script-acceptance.md](references/voiceover-script-acceptance.md) 完成“后台事实 → 创作者表达”转换。资料来源、截图缺失、文字还原、证据分级、镜头选择和制作计划留在后台矩阵与验收记录；成片只说观众需要的任务、动作、结果、条件和创作者判断。建立一次“本期拆解官方实践”的来源背景后，不再逐句重复“官网说、官网给了、官网没图、按官网文字还原”。没有图片时直接用 MG 表达流程；只有证据缺口会改变观众对结果的判断时，才把它改写成自然限制。用户给出的个人工作流和创作观点要真正融入正文，但必须分清产品原生能力、自定义接入、外部工具、手动导入和创作者后期调整。
   社媒口播还必须完成两段不同目的的互动设计：前三秒先用信息差、问题或后果抓住观众，再在价值承诺之后放开头动作钩子；长、密、可反复查询的内容优先用“先收藏 + 以后回来查”的理由，不能把“视频很长”放在第一句劝退。用户的固定记忆句为“先赞后看，养成习惯”，每期只出现一次，放在具体价值承诺之后、正文之前；不得放进第一句或前三秒，也不得与“点赞关注收藏转发”叠成口令。可以紧接一句与当期内容相关的回看收益。片尾先完成结论，再给一个低门槛、与内容直接相关的评论问题；下一期未确定时，默认让观众评论“想看的功能/工具 + 最想解决的问题”。开头和片尾各只要求一个主要动作，不堆“点赞、关注、收藏、转发、评论”，也不使用没有收益理由的乞求式 CTA。
4. 取得原声并建立唯一主时钟：优先使用用户提供的原始音频；若已配置用户授权音色 API，则直接按口播定稿生成、下载并保留 API 原始输出。必须先锁定最终旁白，再按真实发声位置反标口播；不得在音频生成前按字符数量、句子长度、图片数量或预设页长估算时码。字幕、场景、截图、MG 事件和音效必须共用同一份最终音频时间轴。详细规则见 [references/audio-and-timing.md](references/audio-and-timing.md) 与 [references/semantic-timeline-and-sync.md](references/semantic-timeline-and-sync.md)。
5. 把最终旁白拆成“完整语义窗”，再在窗内标记“短语/关键词发声锚点”。语义窗负责一个完整问题、动作或判断；锚点负责触发拖入、点击、字段组合、状态改变、结果出现和截图聚焦。不能只给一大段口播一个起止时间后在段内平均轮播画面。60–120 秒内容通常使用 6–9 个主场景；长视频、多案例或多子流程按实际语义窗数量组织，不得为了套模板压缩步骤或机械平均时长。
6. 为每个语义窗建立可验收的声画合同：`viewerNeed`、`spokenClaim`、`objects`、`action`、`startState`、`endState`、`resultEvidence`、`anchors`、`holdUntil` 与 `coverageStatus`。在选素材或套动效前，必须再填写 `directorPlan`：`attentionTarget`、`visualBeat`、`payoff`、`treatment`、`treatmentReason`，明确观众第一眼看什么、随哪句口播发生什么变化、窗口结束时看懂什么，以及为什么选截图、MG、生成情境或混合呈现。`directorPlan` 不能只是改写口播。官方案例中的每个 `step` 还必须绑定最终旁白时段、对应画面、场内动作和结果状态；任一项缺失、时段不是来自真实音频、截图没有证明对象、关键动作没有发声锚点，均不得进入正式渲染。覆盖表规则见 [references/story-and-motion.md](references/story-and-motion.md)，视觉导演与素材规则见 [references/visual-direction-and-assets.md](references/visual-direction-and-assets.md)，官方案例闸门见 [references/official-case-explanation.md](references/official-case-explanation.md)。依次运行 `scripts/validate_narrative_plan.py <brief.md> <timeline.json>`、`scripts/validate_av_timeline.py <timeline.json>` 与 `scripts/validate_visual_plan.py <brief.md> <timeline.json> --stage directing`。
7. 导演闸门通过后，再倒推并取得每窗真正需要的素材：官方 Logo／官网公开原图／实机截图负责产品身份和事实证据，MG 负责对象、动作、关系和状态变化，生成图片只负责现实情境或氛围。找不到现成图时，应去官方页面截图、实机截取、绘制解释型 MG 或生成非品牌情境，不能因为模板只有卡片就让内容迁就模板。产品第一次被念到时，官方 Logo、官方产品界面或真实产品截图必须在发声前 0.20 秒至发声后 0.35 秒内变得可识别，并稳定至少 0.80 秒；通用 Agent 圆点、字幕或生成的伪 Logo／伪界面不算产品识别。素材取得后运行 `scripts/validate_visual_plan.py <brief.md> <timeline.json> --stage assets`。
8. 新工程默认使用 Remotion 的 React/HTML/CSS/SVG 组件和全局帧时钟；已有 Canvas 或等价确定性工程保留 `renderAt(t)` 接口，两者均按同一绝对时间逐帧渲染，避免依赖墙钟时间的录屏导致掉帧或音画漂移。任何删停顿、替换句子或保持音高的变速，都要通过同一编辑映射同时重算口播句、语义窗、锚点、字幕、截图和音效时间；不得手工只挪其中一层。官网截图按照“全局定位 → 操作区域 → 动作发生 → 结果验证”推进，局部放大只在当前发声锚点需要观众核对具体按钮、字段或结果时出现。两张文字型截图默认上下全宽，三张以上按发声锚点逐张接力；禁止四宫格缩略图和无证明价值的画中画。窗口切换不得把两个完整页面交叉叠化成半透明脏帧；新窗第一帧必须已有可识别主对象，或保持上一完整状态直到新对象就绪。不得出现错页闪帧、空白闪帧和半成品状态。公开官网图不自动打码。详细规则见 [references/story-and-motion.md](references/story-and-motion.md)、[references/semantic-timeline-and-sync.md](references/semantic-timeline-and-sync.md) 与 [references/visual-direction-and-assets.md](references/visual-direction-and-assets.md)。
9. 烧录同步字幕，加入稀疏的语义音效；没有音乐授权时交付无背景音乐母版并给平台内选曲建议。
10. 分别制作封面和发布包。制作封面时必须由当前主智能体组合使用 `$produce-social-media-cover`：每次读取其当前 `SKILL.md` 及它为所选方向指定的参考，不是委派子智能体，也不是读取本 Skill 的旧摘要。封面 Skill 统一负责方向选择、人物身份/姿态/衣着、无字主视觉、确定性排版、三比例重构与专项终验。本 Skill 只交接本期主题、受众痛点、唯一结论、事实/承诺边界、已批准口播、认可的视频帧/色盘/材质、用户人物参考与偏好、所需平台比例，并核对返回封面与视频承诺一致。交接和回收字段见 [references/cover-art-direction.md](references/cover-art-direction.md)，交付检查见 [references/delivery-and-qa.md](references/delivery-and-qa.md)。不得把视频首帧当作正式封面。封面 Skill 不可读取或缺少必要身份参考时，报告缺口并停止该阶段，不用通用模板顶替。封面设计规则只维护在封面 Skill，本 Skill 不固定美漫、配色、服装或姿势。
11. 正式渲染前执行发布洁净检查：画面工程、字幕和封面不得带内部制作状态；可运行 `scripts/check_publish_clean.py <画面工程> <字幕文件>`。命中内部状态词时先返修，不能带病导出。
12. 先输出带绝对时间码、语义窗 ID、当前锚点、活动实体和活动素材 ID 的调试版；检查每个窗的起点、中点、终点和每次状态切换，再移除调试层输出发布版。官网证据长片还要对每个锚点前后和每个语义窗边界抽取实际成片帧，分别生成锚点联系表与边界联系表；可先运行 `scripts/build_anchor_audit_manifest.py <timeline.json> <audit-manifest.json>`，由同一主智能体按清单中的连续区间依次完成全部检查。逐帧检查证据完整性、标注是否精确指向当前词／按钮／表格行、图文碰撞、重复、MG 真实触发和声音接缝。再对完整成片做异常快切检测；任意相邻显著变化间隔小于约 `0.55s` 的位置都必须抽取前后连续帧，排除错页闪一下、动画重置和重复卡一下。详细门槛见 [references/rendered-frame-and-audio-audit.md](references/rendered-frame-and-audio-audit.md)。以 1× 速度完整播放一次声音、字幕、动作、停顿和截图证据；未完整播放不得宣称终验通过。
13. 完成技术、内容、视觉动效、字幕音频四类验收后，记录并批准 `audit`；最终目录清理和复验完成后才能批准 `delivery`。不得用旧版验收报告批准新版成片。

## 返工与样片闸门

- 创作者明确反馈“无聊、拖沓、音画不符、画面卡顿、字幕或发音异常”时，当前版本即失败；不得用编码、响度、帧率或信息完整性反驳。
- 问题涉及叙事骨架、普遍同步或重复视觉模板时，不继续在旧成片上局部打补丁。保留事实证据与原始素材，重写口播、时间轴和视觉逻辑。
- 经过一次整片失败后，先制作约 30–60 秒、包含钩子和至少一个完整证据链的样片。样片必须完成口播、音色、字幕、逐句同步和实际播放检查；样片未确认方向前不批量渲染长片。
- 不论是否返工，具体产品长片在整片渲染前必须先输出 0–8 秒开头样片，以及 0.0、0.8、1.6、2.4、3.0 秒五帧联系表。静音时必须能认出产品并说出开头提出的问题；声画一起看时，产品名、关键动作和信息回报必须落在对应发声锚点。任一项不通过，不得用后文完整度抵消。

## 不可退化的制作标准

- 不是静态图片切换、联系表、PPT 推拉或随机漂浮元素。
- 连续三个场景不得复用同一“标题 + 大图 + 固定放大框 + 说明卡”构图。相同视觉语法可以形成系列感，但每场核心动作必须随口播发生不同的状态变化。
- 截图放大不是装饰。每个裁切区域必须在覆盖表中记录来源、目标区域、证明主张和出现时间；不得用固定比例裁剪不同长宽比图片，也不得截断正在证明的文字、表格字段或前后关系。
- 官网证据上不允许出现无明确目标的黄色／绿色／灰色长条或遮罩。强调必须与当前发声锚点一一对应，使用不遮字的红圈、短箭头、下划线或光标，并保留句首、句尾、表头和页面身份。标注线、箭杆和箭头的可见像素不得与正文、数字、图标、按钮或结果证据相交；圈选必须包围完整对象并留出可见间隙，箭头只停在圈选或目标组件的外边缘。空间不足时改用四角括号、外置短标签、基线外下划线或局部放大，禁止强行画一个压字的圈。
- 截图容器默认 `contain`；`cover` 必须对实际渲染帧证明没有二次截字。证据放不下时按发声锚点分步接力，不得用左右半屏小图完成数量任务。
- 背景、边框、图标、文字和状态必须整体入场；无字底板、极淡空壳、图标和文字交叉、图片压标题，任一项即失败。
- 内容切换时至少保留一个完整可读状态。除明确章节转场外，主证据区不得整体淡到近乎空白、闪白或黑场；交叉切换要先让新状态可识别，再移除旧状态。
- 动效只用于：需求出现、命令/步骤被调用、状态改变、结果回写、关系建立和章节切换。
- 每个语义窗先定义“对象从什么状态经过什么动作变成什么结果”。固定四格加文字顺序出现不算独立动效；连续三个窗不得只换文案而复用同一核心动作。
- 最终视频默认不显示右上角创作者账号名、Logo 或品牌胶囊；只有用户在当期明确要求时才恢复。系列名、期数或主题角标可以保留，但不得把创作者账号标识换个位置重新塞回画面。删除品牌元素后必须重新放大、移动或重排保留内容，不能留下空洞、占位或失衡留白。
- 版式规整是发布底线：系列角标、标题、卡片、字幕框以及用户当期明确要求保留的 Logo 使用明确的共同网格；图标与文字既要几何居中，也要按实际渲染结果检查视觉重心。不能因为代码写了 `center` 或 `align-items:center` 就判定通过。保留的 Logo/角标须把“图标＋文字”作为一个组合整体居中，不能分别用固定坐标摆放；组合的可见像素不得被胶囊、蒙版或画布裁切，左右与上下光学留白明显不均即为发布阻断缺陷。此处约束视频画面；封面专项规则统一见封面 Skill。视频具体修正与抽检方法见 [references/story-and-motion.md](references/story-and-motion.md) 与 [references/delivery-and-qa.md](references/delivery-and-qa.md)。
- 字幕采用居中或左对齐都可以，但必须是明确且全片一致的选择。字幕文字、识别图标、边框和上下内边距要落在同一视觉中线；两行字幕不能出现偶然偏左、偏上或一边留白明显更大的情况。
- 流程图、关系线和箭头必须闭合：每一条线都有可见起点与终点，或有清楚的“继续”标记。箭头只服务一个明确目标，优先短直线或一次规整折线，箭杆、箭头和装饰副笔不得穿过文字、图标、卡片或其他连线，也不得彼此交叉。箭头不得指向画布外、空白区或尚未显示的节点；否定或不可用状态使用独立状态标识，不用斜线穿过原文。内容放不下时重排、分场或删线，不能留下像半成品的连接符。使用圆头线帽的箭头、勾选或路径动画在 `progress=0` 及近零长度阶段必须完全不挂载；只有首段长度明显大于线帽直径后才允许显示，禁止把零长度路径渲染成孤立圆点。
- 时间同步不等于内容同步：场景标题、核心画面和同一时段口播必须回答同一个问题。主画面不得新增口播未解释的选择规则、步骤、能力、结论或限制；缺少口播覆盖时应删除、降为非关键辅助信息，或明确要求补录。
- 视觉丰富不等于堆素材。每窗的素材必须能完成 `attentionTarget → visualBeat → payoff`；不能说明当前口播、动作或结果的图片和装饰动画一律不加。
- 生成素材只允许承担 `explanation` 或 `atmosphere`。不得承担产品 `identity` 或事实 `evidence`，不得生成官方 Logo、仿真产品界面、看似真实的执行结果或数据；品牌元素与产品 UI 必须由官方资源或真实截图确定性叠加。每个非证据型语义窗都要主动判断是否需要生成情境图来建立人物、空间、情绪或现实任务，不得默认退化成文字卡。尤其当相邻画面视觉骨架相似时，应优先用新的情境图、不同空间关系或不同视觉隐喻拉开差异；生成图无助于当前 `attentionTarget → visualBeat → payoff` 时才不使用。
- 相邻语义窗必须在背景、构图、主对象尺度、证据类型或运动语法中至少有一项形成一眼可辨的变化。上一窗的背景、截图或卡片如果会让新窗看起来只是盖了一层，必须完整离场；半透明继承上一屏、重复同一中央大卡、只换标题或只重播入场动画均为阻断级失败。
- 红圈、圆角框和括号必须从当前目标的实际 `contentBounds` 动态计算，按目标长宽和安全间隙自适应，禁止把一个固定大小的圈复用到不同文字或控件。来源截图自身若含页码压标题、浮层遮字、截断原文、暗屏或无关阴影，先裁切、重构排版或在不改变事实的前提下重绘受损标题；不得再叠暗幕掩盖问题。
- 抽象表达改成用户能立即理解的动词，例如“约时间、找资料、改数据、走流程”。
- 主画面与字幕区分层；常用竖屏底部预留约 220–300px，避免与平台 UI 和 CTA 重叠。
- 画面出现真实命令、数据、接口或产品能力时必须可核验；组合流程写“示例/目标/可组合”，不能暗示已端到端跑通。
- 封面专项标准以 `$produce-social-media-cover` 当前内容和所选方向为准，不在本 Skill 复制维护。必须实际读取、制作、看图并取得各所需比例的专项验收，不能仅因文件存在或引用了名称就宣称通过。同片封面无需通过美漫模式检查；共用的发布洁净、事实边界和平台安全区仍须满足。仅改封面时不擅改批准口播、音频或视频；方向预览通过不能直接批准整个 `cover` 或 `delivery` 阶段。
- 可直接发布的成片和封面必须是“观众视角的干净画面”。不得显示 `已配原声`、`正式版`、`待原声`、`视觉预览`、`草案`、`测试版`、`候选版`、`调试` 等内部制作状态、审核水印或工作流标签。预览状态只能写在预览文件名、项目记录或验收文档里；进入最终导出前必须从画面层删除。
- `示意`、`取决于具体模型`、AI 内容标识、广告/版权声明等事实边界或平台合规信息不是内部状态；只有观众理解或合规确有需要时才保留。
- `示意` 可以作为简短的屏幕边界标签，但不应机械进入口播。口播要保留事实含义，用自然人话说明差异、条件和限制；屏幕标签不能替代必要解释。
- 最终交付的“中文口播稿”只含标题和观众实际听到的正文；合成参数、稿音对应、系列锁定、制作说明和“不朗读”备注移入验收报告或项目记录，不能混在发布稿里。
- 场景页码、镜头编号、英文 kicker、模板栏目名和极小制作备注只有在帮助观众定位或理解时才可出现；纯粹为了“像模板”或方便制作的标签一律从成片删除。

## 原声与字幕底线

- 永远保留原始音频副本。
- API 合成返回的原始文件视同原声母本：先保存并记录哈希、生成模型/版本、音色标识、生成时间和口播稿版本，再做剪辑、响度或变速处理。
- 不把“请求已发出”当作音频完成。必须确认响应为可解码音频、文件非空、时长合理；异步接口要等任务成功且结果下载完成。
- 口播定稿变更后不得沿用旧音频；按新稿重新生成，并保留可追溯的稿件—音频对应关系。
- 默认只从明确静音的中段收紧气口；保留语义停顿，剪点靠近零交叉或使用短交叉淡化。
- 社媒解释口播默认追求“自然但紧凑”：先删重复铺垫和可从画面读出的复述，再收紧异常长停顿，最后校准整体语速。不得用变速掩盖冗长稿件，但也不得因为保守限速而交付明显拖沓、像刻意慢读的旁白。
- 普通句间停顿通常约 0.16–0.30 秒，章节转折约 0.30–0.45 秒；检测到超过约 0.55 秒的非强调静音时应逐处复核并优先只删中段。
- 中文信息型口播的字数/分钟只作相对诊断，不设统一目标；创作者明确反馈“慢、不自然”时视为未通过听感验收，不能用字数或技术指标反驳。
- 原声偏慢时至少试听原速、约 1.20×、1.35× 三档；1.35× 仍拖沓时继续试听约 1.50×。约 1.50× 可以成为正式速度，不再以 1.20× 作为绝对上限。选择听起来最自然、清楚、紧凑的一档，而不是机械选择最高倍速；英文缩写、句尾、数字或情绪明显发紧、吞字、含混、金属化时回退或局部调整。
- 变速必须保持音高，并记录原始时长、停顿编辑后的时长、所选变速系数和最终时长。任何删停顿或变速都要重新计算字幕、场景边界和音效点，再做响度与真峰值处理。
- 成片旁白建议约 -16 LUFS，真峰值不高于 -1.5 dBTP；音效必须明显低于人声。
- 字幕以逐句/逐词发声锚点为基础，再用口播定稿纠错；不得把品牌名和英文词错误拆行。
- 合成输入保留中文与英文、数字、缩写之间必要的空格和停顿；不得为了“清理文本”删除所有空白。先建立专有名词表并抽检发音，长段落优先按完整语义句生成，避免一整段平读或跨句粘连。
- 不得为了取得字幕时码，把一句自然口语拆成多个互不连贯的 TTS 请求。默认以完整语义句或能一口气说完的短段为合成单元；字幕需要更细时优先使用发声锚点/forced alignment，不能让字幕切分反过来破坏语气、重音和口音一致性。
- 同一授权音色出现地区口音漂移、语气反复重置或断句像拼接时，当前音频即失败。用同一段自然样句显式比较官方当前支持的高质量与低延迟模型，先选稳定模型，再合成长稿；不得用整体加速、硬切或字幕掩盖口音与语气问题。
- 禁止用字数权重把字幕平均铺满音频。ASR 只提供时间锚点，发布字幕必须按定稿纠错并按语义断句；不得出现半个词留在上一条、下一条从单字或标点开始、品牌名被拆开。
- 最终旁白必须从头到尾完整试听一次，并额外复核中英混排、数字、专有名词和片尾。出现重复音节、卡顿、错音、吞字、截断、异常停顿或句间拼接感时，重新合成受影响句子或全稿；不得靠加速、降噪或字幕掩盖。音频变化后重新生成时码、字幕和场景边界。
- 无法实际听完整音频时不得宣称“旁白听感通过”；至少完成 ASR 对稿、专有名词清单与两个节奏候选，并把最终听感确认留给创作者。

## 交付与授权

最终按平台输出中文命名文件，至少包括成片、封面、口播稿、SRT 和发布包。平台要求横封面时同时输出 3:4 竖封面与 4:3 横封面。具体清单见 [references/delivery-and-qa.md](references/delivery-and-qa.md)。

公开发布属于外部操作：只有用户明确要求发布，并在看过最终成片、封面和文案后，才执行上传或发布。制作授权不等于发布授权。

## 可复用资源

- 新建项目时可运行 scripts/init_project.py <project-dir> --title "主题" --mode information|concept|demo。
- 已有项目可运行 `python scripts/project_state.py init <project-dir>` 建立单主智能体状态账本；各阶段用 `record`、`approve` 和 `check --through <stage>` 管理版本与失效传播。
- MossAPI 已配置时可运行 `python scripts/moss_tts.py synthesize --input-file <口播稿> --output <原始音频>`；音色从 `MOSS_VOICE_ID` 或 `MOSS_VOICE_NAME` 解析，密钥从 `MOSS_API_KEY` 读取。
- 首次核对音色可运行 `python scripts/moss_tts.py list-voices --match <音色名称>`，确认返回的名称与 ID 后再生成完整口播。
- 完成后运行 scripts/validate_delivery.py <output-dir> --topic "主题" 做交付结构与基础媒体检查。
- 正式渲染前运行 `python scripts/check_publish_clean.py <render-source> <final-srt>`，避免把内部状态词烧进可发布画面。
- 语音合成前运行 `python scripts/check_audience_language.py <spoken-script> <final-srt>`，拦截被误写进口播的制作说明，并人工复核脚本提示的术语。
- assets/remotion-template/ 是默认的可测试 Remotion 画面起点；`scripts/prepare_remotion.py` 校验并编译同一时间轴，原有步骤 8 的绝对秒数通过统一帧映射实现，不额外创建第二套时间源。
- assets/mg-template/ 仅用于显式选择 `--renderer canvas-legacy` 的已有 Canvas 兼容；不能绕过新增测量、避让和实际帧检查。
- assets/templates/ 包含 Brief、时间轴和发布包模板。

确认成品可用后，清理拆帧、联系表、候选版、临时混音和调试文件；保留最终交付、用户原始素材、事实证据与可复现的时间轴/工程文件。
